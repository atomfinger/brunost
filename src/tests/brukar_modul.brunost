bruk terminal

modul rekning {
  gjer dobbel(tal) {
    gjevTilbake tal * 2
  }
  gjer kvadrat(tal) {
    gjevTilbake tal * tal
  }
}

terminal.skriv(rekning.dobbel(5))
terminal.skriv(rekning.kvadrat(4))
