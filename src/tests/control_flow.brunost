bruk terminal

endreleg verdi er 1

viss (verdi er 1) gjer {
  terminal.skriv("ein")
} ellers viss (verdi er 2) gjer {
  terminal.skriv("to")
} ellers {
  terminal.skriv("anna")
}

endreleg tal er 10

viss (tal > 5) gjer {
  terminal.skriv("stort")
} ellers {
  terminal.skriv("lite")
}
