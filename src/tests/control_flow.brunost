bruk terminal

open verdi er 1

viss (verdi er 1) gjer {
  terminal.skriv("ein")
} elles viss (verdi er 2) gjer {
  terminal.skriv("to")
} elles {
  terminal.skriv("anna")
}

open tal er 10

viss (tal > 5) gjer {
  terminal.skriv("stort")
} elles {
  terminal.skriv("lite")
}
